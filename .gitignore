# LaTeX build artifacts — local builds now collect these under build/
build/
*.aux
*.log
*.out
*.toc
*.fls
*.fdb_latexmk
*.synctex.gz
*.bbl
*.blg
*.run.xml
*.bcf
*.nav
*.snm

# Built PDF — generated by CI. To also version the rendered PDF in git,
# remove the lines below.
resume.pdf
apps/web/assets/resume.pdf

# Tailored per-JD résumé outputs (personal, regenerated on demand)
tailored/

# Personal LinkedIn source + working drafts — never publish to this public repo.
# The raw export stays local; the scraper distills it into profile/linkedin.json.
Linkedin_Profile.pdf
linkedin-updates.md
# Standing Wellfound profile draft (generated by `pnpm wellfound-profile`).
wellfound-profile.md

# Node / Vercel
node_modules/
.vercel
.env
.env*.local

.vercel

coverage/
dist/

# turbo local cache
.turbo/