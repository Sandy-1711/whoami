# LaTeX build artifacts — local builds now collect these under build/
build/
*.aux
*.log
*.out
*.toc
*.fls
*.fdb_latexmk
*.synctex.gz
*.bbl
*.blg
*.run.xml
*.bcf
*.nav
*.snm

# Built PDF — generated by CI. To also version the rendered PDF in git,
# remove the lines below.
resume.pdf
apps/web/assets/resume.pdf

# Tailored per-JD résumé outputs (personal, regenerated on demand)
tailored/

# Personal working drafts — never publish to this public repo.
wellfound-profile.md

# Personal LinkedIn source — the raw export stays local.
# The scraper distills it into profile/linkedin.json.
Linkedin_Profile.pdf
linkedin-updates.md

# Agent workspace — chat memory, application tracker, feedback, stored JDs, and
# the derived embedding cache. All personal + machine-local; never committed.
.agent/
# Build progress log — hand-off state between chat sessions, never committed.
AGENT_BUILDLOG.md

# Node / Vercel
node_modules/
.vercel
.env
.env*.local

.vercel

coverage/
dist/

# turbo local cache
.turbo/