# Workspace packages resolve each other by the workspace: protocol.
link-workspace-packages=true
auto-install-peers=true
# Don't fail `pnpm run` on esbuild's optional (unneeded) build script — its
# platform binary ships as an optional dependency, so tsx/vitest work regardless.
verify-deps-before-run=false
